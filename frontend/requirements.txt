# Streamlit 1.57+ needs a newer Starlette; pin below that for a stable local install
# and prefer a dedicated frontend/.venv (avoid clashing with global packages).
streamlit>=1.40.0,<1.57
httpx>=0.27.0
plotly>=5.24.0
pandas>=2.2.0
